/*
package com.laawe.purchasing.auth.repository.impl;

import com.laawe.purchasing.auth.model.entity.M_User;
import com.laawe.purchasing.auth.repository.UserRepository;
import org.springframework.stereotype.Repository;

import java.util.Optional;
import java.util.UUID;

@Repository
public class UserRepositoryImpl implements UserRepository {
    @Override
    public Optional<M_User> findByEmail(String email) {
        return Optional.empty();
    }

    @Override
    public Optional<M_User> findByIdf(UUID idf) {
        return Optional.empty();
    }

    @Override
    public boolean existsByUsername(String username) {
        return false;
    }

    @Override
    public boolean existsByEmail(String email) {
        return false;
    }

    @Override
    public Optional<M_User> findByUsername(String username) {
        return Optional.empty();
    }
}
*/
